# Update pip to the latest version before installing the packages
--upgrade pip

# Install the python-dotenv package
python-dotenv

# Main libraries for deep learning (GPU support via CUDA)
torch
torchvision
torchaudio
-f https://download.pytorch.org/whl/torch_stable.html

# Hugging Face and NLP tools
transformers
datasets

# GPU optimization and quantization libraries
accelerate
bitsandbytes

# Environment variable management
python-dotenv
